# Read-only Sanity token. Only needed if the Sanity dataset is private.
# Get one at https://www.sanity.io/manage/personal/project/wox2571x/api
#   → API tokens → Add API token → name: "site-read", permissions: Viewer
# Then copy the token here as .env.local (gitignored) OR store as
# GitHub Actions secret SANITY_READ_TOKEN.
SANITY_READ_TOKEN=
